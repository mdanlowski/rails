<h1>Sessions#new - Login Page</h1>

<p>
	<h3>Please provide username and password to log in.</h3>	
</p>

<div class="errors">
	<% if flash[:login_errors] %>
		<% flash[:login_errors].each do |e| %>
			<p> <%= e %> </p>
		<% end %>
	<% end %>
</div>

<%= form_for :session, url: { action: 'create' } do |s| %>
	<p>
		<%= s.label :username %>
		<br />
		<%= s.text_field :username, placeholder: 'Username'  %>
	</p>
	<p>
		<%= s.label :password %>
		<br />
		<%= s.password_field :password, placeholder: 'Password'  %>
	</p>
	<p>
		<%= s.submit %>
	</p>

<% end %>
