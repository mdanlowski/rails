import React from 'react'
import ReactDOM from 'react-dom'
import App from '../components/App'

const app = document.getElementById('root');
ReactDOM.render(<App/>, app); // render Layout into app and that's itf